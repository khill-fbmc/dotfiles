export NVM_DIR="$HOME/.nvm"

local NVM="/opt/homebrew/opt/nvm"

[ -s "$NVM/nvm.sh" ] && source "$NVM/nvm.sh"  # This loads nvm
[ -s "$NVM/etc/bash_completion.d/nvm" ] && source "$NVM/etc/bash_completion.d/nvm"  # This loads nvm bash_completion
